version: "3"
services:
  build_mkdocs:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./unbrake:/unbrake
    command: mkdocs build

  serve:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./unbrake:/unbrake
    command: mkdocs serve 
  
  deploy:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./unbrake:/unbrake
    command: mkdocs gh-deploy 

